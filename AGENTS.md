# AGENTS.md

이 저장소에서 작업하는 Agent는 아래 우선순위를 따른다.

```text
사용자의 최신 명시 지시
> 이 파일
> docs/foundation/GAME_CONTRACT.md
> 프로젝트 전용 Skill
> 관련 설계·기술 문서
> GamePlanner / UI_UX_Skill_for_Game 범용 코어
> 일반적인 장르 관습
```

## 세션 시작

1. `.game-wiki/current-state.md`를 읽는다.
2. `.agents/skills/onemanarmy-production-router/SKILL.md`로 현재 요청을 분류한다.
3. 요청과 관련된 프로젝트 전용 Skill만 추가로 읽는다.
4. 기준 branch, HEAD SHA, 미커밋 변경, 실제 존재 경로를 확인한다.
5. 구현 요청이면 Work Order와 검증 기준이 있는지 확인한다.

## 절대 훼손하지 말아야 할 계약

- 이연은 첫 장면부터 완성된 강자다.
- 108검은 후반 해금 보상이 아니라 작품의 첫 번째 상품이다.
- 108검은 `12개 검대 × 9검`의 질서 있는 군진이다.
- 이연은 일반 전투에서 HP·마나 부족으로 보잘것없어지지 않는다.
- 긴장은 전투 승패보다 동시에 달성할 목표와 승리의 대가에서 발생한다.
- 추리와 음모는 누구를 어떤 검진으로 처리할지 결정하는 정보 수집이다.
- 하드보일드는 표면 장식이 아니라 개인 규칙, 썩은 세계, 폭력의 결과로 표현한다.
- 다회차는 주인공의 능력치를 재설정하지 않고 힘의 사용 원칙을 바꾼다.
- 모든 대형 전투는 `이연의 작은 동작 → 검진의 거대한 변화` 대비를 가진다.
- UI는 검의 수를 세게 만드는 장식보다 현재 목표·배치·결과를 명확히 보여야 한다.

## 금지 구조

- 이연이 매 전투 상단전 과부하로 쓰러지는 구조
- 적이 반복적으로 108검의 조종권을 빼앗는 구조
- 108검이 단순 유도탄·총알처럼 직선 발사되는 연출
- 전투마다 더 강한 적이 검을 무효화하는 파워 인플레이션
- 호감도 수치만으로 루트가 갈리는 연애 공략 구조
- 장편 추리 대화가 중심이고 108검은 마지막에만 나오는 구성
- 첫 회차에서 108검을 숨기고 진엔딩에서 공개하는 구성
- UI 작업에 전투 규칙·밸런스 변경을 몰래 포함하는 것
- 프로토타입 검증 전 전체 시나리오·전체 자산을 대량 생산하는 것

## 요청별 필수 Skill

| 요청 | 필수 Skill |
|---|---|
| 전체 방향, 우선순위, 다음 작업 | `onemanarmy-production-router` |
| 핵심 판타지, 범위, 금지 구조 | `onemanarmy-foundation` |
| 시놉시스, 장면, 인물, 루트, 대사 | `onemanarmy-story-route-director` |
| 전투, 검진, 108검, 카메라, VFX | `onemanarmy-formation-director` |
| 대화창, 전술 화면, 메뉴, 입력, 접근성 | `onemanarmy-ui-ux` |
| Godot 코드, 씬, 데이터, 테스트, 성능 | `onemanarmy-godot-director` |

복합 요청은 라우터가 가장 작은 안전 순서를 정한다. 모든 Skill을 무조건 읽지 않는다.

## 구현 전 게이트

제품 코드 변경에는 다음이 필요하다.

- 명확한 목표
- in-scope / out-of-scope
- source-of-truth
- 관찰 가능한 수용 기준
- 검증 명령 또는 대체 증거
- 롤백 기준
- 위험도가 medium 이상이면 영향 범위

현재 런타임 코드가 없으므로 초기 프로토타입은 `throwaway`와 `evolutionary` 부분을 분리한다. 렌더러·데이터 모델은 승격 가능하게 만들 수 있지만, 임시 스테이지·임시 대사는 제품 자산으로 간주하지 않는다.

## 문서와 구현의 관계

- `docs/foundation/`은 게임의 정체성과 범위를 규정한다.
- `docs/design/`은 플레이 규칙과 콘텐츠 계약을 규정한다.
- `docs/ui/`는 플레이어 질문과 화면 행동을 규정한다.
- `docs/technical/`은 구현 경계와 데이터 권위를 규정한다.
- `.game-wiki/`는 왜 결정했는지와 현재 상태를 기록한다.
- 코드가 문서와 충돌하면 자동으로 코드 또는 문서를 정답 처리하지 않는다. 충돌을 기록하고 상위 계약을 확인한다.

## 완료 선언

다음 표현은 증거 없이 사용하지 않는다.

- 완성
- 직관적
- 재미있음
- 최적화됨
- 출시 가능
- 사용자 검수 통과

정적 검사, 엔진 부팅, 실제 입력, 실제 사용자 테스트를 구분해 보고한다. UI의 최종 승인과 핵심 재미 판정에는 동일 빌드의 실제 플레이 증거가 필요하다.

## 세션 종료

- `.game-wiki/current-state.md` 갱신
- 중요한 결정·수정·실패를 Wiki에 기록
- 완료, 미완료, 검증, 다음 안전 작업, 금지 변경을 handoff에 남김
- 구현했다면 branch, HEAD SHA, 실행한 명령과 결과를 기록
